name: Reusable workflow
on: 
  workflow_call:
    inputs:
      who-to-greet:
        description: Person to greet
        required: true
        type: string
        default: World
    outputs:
        time: ${{ jobs.reusable-job.outputs.time }}
jobs:
  reusable-job:
    name: Reusable job
    runs-on: ubuntu-latest
    outputs:
      time: ${{ steps.time.outputs.time }}
    steps:
      - run: |
          echo "Hello ${{ inputs.who-to-greet }}"
      - id: time
        run: |
          echo "time=$(date)">> $GITHUB_OUTPUT
    
    
